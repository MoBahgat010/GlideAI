# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.2
sentence-transformers>=3.0       # CrossEncoder (bge-reranker-base) for reranking
transformers>=4.40               # jina-clip-v2 via AutoModel

# ── LangChain ─────────────────────────────────────────────────────────────────
langchain>=0.2
langchain-huggingface>=0.0.3
langchain-experimental>=0.0.60   # SemanticChunker
langchain-opendataloader-pdf     # OpenDataLoaderPDFLoader (PDF parsing)

# ── Weaviate ──────────────────────────────────────────────────────────────────
weaviate-client>=3.24

# ── Celery + Redis ────────────────────────────────────────────────────────────
celery[redis]>=5.3
redis>=5.0

# ── NVIDIA NIM / OpenAI client ────────────────────────────────────────────────
openai>=1.30

# ── Rev AI Speech-to-Text ─────────────────────────────────────────────────────
rev_ai>=2.21

# ── FastAPI server ───────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
aiofiles>=23.2
python-multipart>=0.0.9

# ── BM25 ──────────────────────────────────────────────────────────────────────
rank-bm25>=0.2

# ── Image processing ─────────────────────────────────────────────────────────
Pillow>=10.0

# ── S3 (optional — only needed if S3_URI is set) ─────────────────────────────
boto3>=1.34

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0
numpy>=1.26
